{
  "name": "@company/lambda-observability",
  "version": "1.0.0",
  "description": "Zero-config observability library for AWS Lambda functions",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "zip -r lambda-observability-layer.zip .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws-lambda",
    "observability",
    "opentelemetry",
    "tracing",
    "logging"
  ],
  "author": "Platform Engineering Team",
  "license": "MIT",
  "dependencies": {
    "@opentelemetry/api": "^1.7.0"
  },
  "peerDependencies": {
    "@aws-sdk/client-sns": "^3.x.x",
    "@aws-sdk/client-sqs": "^3.x.x",
    "@aws-sdk/client-dynamodb": "^3.x.x"
  }
}